;;; File-local variables

;;; Are variables that emacs can enable per-file if they present in the file. They can appear
;;; as headers
;;; Example 
-*- mode: python ; my-variable: -*-

;;; Variable containing the map of regex-maches -> mode-to-use accordingly
auto-mode-alist ; alist is associative list
